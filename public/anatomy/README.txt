Drop realistic anatomy GLB/GLTF assets in this folder.

Current supported file:
- brain.glb

Expected path:
- /anatomy/brain.glb

Notes:
- The app falls back to the existing primitive brain if this file is missing.
- Keep the model centered near its own origin before export when possible.
- Binary .glb is preferred over .gltf for simpler asset handling in Vite.
